node_modules/
out/
.next/
dist/
cache/
broadcast/
.env
.env.local
.env.*.local
*.log
*.tsbuildinfo
coverage/
# Phase 7 — vault key file (secret; keep out of git)
.vault-key
.vault.key

# Deployer wallet (secret; keep out of git)
.wallet

# Attestation error queue (local runtime state)
.veil/

# Local tool config (not part of the project)
.claude/
.codex/